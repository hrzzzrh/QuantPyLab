# QuantPyLab Code Review Rules

本文件是全局 `code-reviewer` 的项目补充规则。它只增加 QuantPyLab 的架构、数据口径和质量门禁要求，不替代项目根目录 `AGENTS.md`，也不能取消全局审查员的只读、取证、范围和安全边界。

## 变更范围

- 核心代码范围：`storage/`、`backtest/`、`data_ingestion/`、`utils/`、`config/`、`analysis/`、`tools/`、`main.py`、`tests/`。
- `workspace/` 用于开发中的设计稿、研究草案、脚本和实验数据。除非变更直接影响核心代码，否则不把 workspace 内容当作核心生产代码审查。
- 读取或核验数据时，禁止对 `data/` 做全量或递归扫描；使用精准路径、脚本查询或 `metadata.db` 定位数据。
- 所有查询必须通过 `storage/database/views/` 中的 Python 视图类完成，遵循显式依赖 DAG；不得在业务代码中绕过统一视图直接查询底层数据。

## 必须验证的命令

核心代码变更必须执行并记录：

```text
uv run ruff check .
uv run ruff format --check .
uv run pytest tests/
```

- 网络、外部接口、真实数据库和 `data/` 数据湖必须用 `monkeypatch` 或 mock 隔离；测试不得依赖真实网络或真实外部数据。
- 测试失败、静态检查失败、命令未执行或环境依赖缺失必须如实报告，不得写成验证通过。
- 当前审查 agent 只被授权执行 `uv run ruff*` 和 `uv run pytest*`，不执行 `uv run main.py` 或其他 `uv run` 命令。

## QuantPyLab 领域不变量

- 财务与指标计算必须保持披露时间口径，TTM 计算不得使用当时尚未披露的信息。
- 财务数据的归母/扣非、少数股东权益、单位、期间和 TTM 窗口必须前后一致。
- 日线行情、复权因子和后复权净值的口径必须一致，回测成交遵循 T+1 开盘成交规则。
- 增量同步由披露日历或增量边界驱动，重复执行必须幂等；失败或部分写入后不得污染同步状态。
- SQLite 与 DuckDB 双引擎的同一逻辑必须保持结果一致；Parquet 分片路径、元数据和 `metadata.db` 状态必须匹配。
- 任何新增或修改的回测逻辑都必须检查交易日边界、T+1 约束、复权处理、现金/持仓状态和净值计算。

## 架构与项目规范

- 数据获取、存储、分析和回测层的职责必须保持清晰；不得把业务逻辑塞进采集、文件 I/O 或数据库连接代码。
- 新代码应复用现有视图、日期工具、日志、配置和同步状态抽象；只有存在明确差异时才新增同类抽象。
- 函数、类、变量和 CLI 子命令必须准确描述职责；命令遵循“动作-对象”命名，禁止使用 `all`、`export`、`data` 等宽泛名称作为独立命令或核心职责名称。
- 复杂功能变更必须有 `workspace/` 设计文档并遵守提案与确认机制；代码审查不得以代码已能运行替代该流程。
- 核心文档更新必须保持存量内容完整，不得使用 `...`、`(rest of code)` 或其他占位符；行为变化要同步相关 `docs/` 与 `docs/usage.md`。
- 脚本统一使用 uv 环境；用户可执行 CLI 遵循 `uv run main.py <subcommand> [options]`。

## 测试重点

- 采集器：超时、空响应、字段缺失、重复数据、分页边界、重试和部分失败。
- 财务指标：披露日、报告期、TTM 窗口、单位、归母/扣非和未来信息穿越。
- K 线与回测：停牌/缺失交易日、复权因子、T+1 开盘成交、现金与持仓状态、边界日期。
- 存储与同步：事务提交/回滚、幂等写入、同步状态恢复、SQLite/DuckDB 一致性和 Parquet 分片定位。
- 所有测试断言必须验证实际行为，不能只断言 mock 被调用或数据结构仍然存在。

## 不应重复报告

- ruff 已明确报告的纯格式、import 顺序和可自动修复问题，只引用实测结果，不再写成主观风格建议。
- 仅因为个人偏好而要求更换已经正确且符合项目模式的实现。
- 变更范围外、与本次代码没有直接关系的历史技术债；此类内容只能列入“外部建议”。
