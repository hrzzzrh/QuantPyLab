---
name: git-commit-and-push
description: Review, confirm, precisely commit, and push QuantPyLab changes when the user asks to submit or push repository work.
---

# 提交并推送修改

将用户请求作为原命令的 `$ARGUMENTS`。在执行任何操作前，完整读取 `.opencode/commands/gitCommitAndPush.md`，并逐条执行其全部要求。

该文件是唯一流程基准。不得省略、合并、重排或改写其中的前置检查、审查、测试、用户确认、精确暂存、提交和推送步骤。`.opencode/` 只读，不得修改。
